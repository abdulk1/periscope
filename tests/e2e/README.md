# End-to-end tests

`kind`-based integration tests, including the fixture generator that seeds a
cluster with 10,000 pods for the Phase 1 load budget.

Empty in Phase 0: there is no cluster connection to test yet. This directory is
a placeholder so the layout in `IMPLEMENTATION.md` §2.3 exists from the start.
